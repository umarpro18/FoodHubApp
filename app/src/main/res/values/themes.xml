<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.FoodhubAndroid" parent="android:Theme.Material.Light.NoActionBar" />

    <style name="SplashTheme" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/teal_200</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_logo</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/teal_200</item>

        <!-- Optional: specify which theme to use *after* splash -->
        <item name="postSplashScreenTheme">@style/Theme.FoodhubAndroid</item>
    </style>
</resources>